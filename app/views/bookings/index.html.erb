<div class='container'>
  <h1>My Bookings</h1>

  <% if @bookings_fl != nil %>

  <table class='table'>
    <thead>
      <tr>
        <th>Description of booking</th>
        <th>Booking's start date</th>
        <th>Booking's end date</th>
      </tr>
    </thead>
    <% @bookings_fl.each do |booking| %>
    <tr>
      <td><%=booking.enquiry.description%></td>
      <td><%=booking.enquiry.start_date.strftime("%e %b %Y %l:%M %p")%></td>
      <td><%=booking.enquiry.end_date.strftime("%e %b %Y %l:%M %p")%></td>
    </tr>
    <% end %>
  </table>

  <% else %>
    <div>
      <h1>You do not have booking yet.</h1>
    </div>

  <% end %>
</div>
