<h1>Edit Enquiry</h1>

<% if current_freelancer %>
  <%= form_for(@enquiry, url: bookings_create_path, html: {method: "POST"})do |f| %>
  <% if @enquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enquiry.errors.count, "error") %> prohibited this enquiry from being saved:</h2>
      <ul>
        <% @enquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %>
    <%= @enquiry.description %>
    <%= f.text_field :description, class:"enquiry-"+ @enquiry.id.to_s + "-description", disabled: disabled %>
  </div>
  <br>
  <div class="field">
    <%= f.label :start_date %>
    <%= @enquiry.start_date %>
    <%= f.datetime_field :start_date, class:"enquiry-"+@enquiry.id.to_s + "-start_date", disabled: disabled  %>
  </div>
  <br>
  <div class="field">
    <%= f.label :end_date %>
    <%= @enquiry.end_date %>
    <%= f.datetime_field :end_date, class:"enquiry-"+@enquiry.id.to_s+ "-end_date", disabled: disabled  %>
  </div>
  <br>
  <div class="field">
    <%= f.label :price %>: $
    <%= @enquiry.price %>
    <%= f.number_field :price, class:"enquiry-"+ @enquiry.id.to_s + "-price", disabled: disabled  %>
  </div>
  <br>
  <div class="field">
    <%= f.label :params %>: $
    <%= @enquiry.price %>
    <%= f.hidden_field :id, {:class => 'form-control'} %>
  </div>
  <div class="actions">
    <%= f.label "Click me to accept" %></br>
    <%= f.submit "Accept"%>
  </div>
  <% end %>
<% elsif current_user %>
  <%= form_for(@enquiry, :html=>{:remote => true})do |f| %>
  <% if @enquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enquiry.errors.count, "error") %> prohibited this enquiry from being saved:</h2>
      <ul>
        <% @enquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, class:"enquiry-"+ @enquiry.id.to_s + "-description" %>
  </div>
  <br>
  <br>
  <div class="field">
    <%= f.label :start_date %>
    <%= f.datetime_field :start_date, class:"enquiry-"+@enquiry.id.to_s + "-start_date" %>
  </div>
  <br>
  <br>
  <div class="field">
    <%= f.label :end_date %>
    <%= f.datetime_field :end_date, class:"enquiry-"+@enquiry.id.to_s+ "-end_date" %>
  </div>
  <br>
  <br>
  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price, class:"enquiry-"+ @enquiry.id.to_s + "-price" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>
<% end %>


<div id='messages'>
  <%= render 'messages/message'%>
</div>

<form class="new_message" data-behavior="enquiry_speaker">
  <input name="enquiry_id" type="hidden" value="<%= @enquiry.id %>">
  <input name="sender_id" type="hidden" value="<%= current_user.id %>">
  <textarea name="content" class="form-control" ></textarea>
  <!-- <input type="submit" class="btn btn-success" value="Send"> -->
</form>
