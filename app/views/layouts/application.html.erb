<!DOCTYPE html>
<html>
  <head>
    <title>ThirdProject</title>
    <style>
  #map {
    height: 400px;
    width: 100%;
   }
</style>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- <div class="container"> -->
      <%= render 'partials/flash' %>
      <% if current_user %>
       <%= current_user.name.inspect %>
       <% if current_freelancer %>
        <%= current_freelancer.inspect %>
        <% end %>
      <% end %>

      <!---UNCOMMENT ME FOR BOOTSTRAP STYLE -->
      <!-- <div class="header clearfix">
        <nav>
          <ul class="nav nav-pills pull-right">
            <li><%= link_to '#' %></li>
            <li><%= link_to '#' %></li>
            <li><%= link_to '#' %></li>
            <li><%= link_to '#' %></li>
            <li><%= link_to 'Home', root_path, :role => 'presentation', :class => 'active'%></li>
          </ul>
        </nav>
        <h3 class="text-muted">Jobs that can only be done with many Hands</h3>
      </div>

      <div class="row">
        <div class="col-lg-12"> -->
          <%= yield %>
          <%= debug(params) if Rails.env.development? %>
        <!-- </div>
      </div>

      <footer class="footer">
        <p>&copy; 2017 Handys, Inc.</p>
      </footer>

    </div> <!-- /container -->
  </body>
</html>
