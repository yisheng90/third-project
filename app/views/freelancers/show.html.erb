<% if current_freelancer %>
  <h1>Freelancer's dashboard</h1>
  <h3><%= link_to 'edit your profile!', edit_profile_path(@freelancer) %>
  <h3>Current Overall Rating</h3>
  <%= render partial: 'partials/form_for_ratings', locals: {big_rating: @compiled_rating, cool_var: 'hey gurl'} %>
  <h3>Pending enquires</h3>




  <table>
    <% @enquiries.each do |enquiry| %>
      <tr>
        <td><%=enquiry.user_id%></td>
        <td><%=enquiry.description%></td>
        <td><%=enquiry.start_date%></td>
        <td><%=enquiry.end_date%></td>
        <td><%= link_to 'view enquiry', enquiry_path(enquiry.id) %></td>
      </tr>
    <% end %>
  </table>


  <h2>View Your Schedule</h2>
  <h3>Your Work <%= @freelancer.schedule %></h3>
  <h3>Edit your profile to change your schedule</h3>
  <%= month_calendar(partial: 'simple_calendar/month_calendar', events: @occurrences[:dates] ) do |date, occurrences| %>
    <%= date %>
    <!-- if date is equivalent to the enquiry's booking start date -->
    <% if date == @occurrences[:start_booked_times].to_date %>
    <h3>Slot 1 from <%= @occurrences[:start_booked_times] %> to <%= @occurrences[:end_booked_times] %>  </h3>
    <!-- <%= @occurrences[:start_booked_times] %>
    <%= @occurrences[:end_booked_times] %> -->
    <% end %>
    <% occurrences.each do |occurrence| %>
    <div>
      Available from <%= @sanitized_start_time %> to <%= @sanitized_end_time %>
    </div>
    <% end %>
  <% end %>


<% else %>
  <h1>hello world</h1>
  <h3>name: <%= @freelancer.user.name %></h3>
  <h3>profession: <%= @freelancer.profession %></h3>
  <h3>description: <%= @freelancer.description %></h3>
  <h3>picture: <%= @freelancer.picture %></h3>
  <h3>price range from <%= @freelancer.price_start %> to <%= @freelancer.price_end %></h3>
  <%= link_to 'Click here to rate me!', rating_path %>

  <%= link_to 'new enquiry', new_profile_enquiry_path(@freelancer)%>
  <h3>Current Overall Rating</h3>
  <!-- do some algo magic here -->
  <%= render partial: 'partials/form_for_ratings', locals: {big_rating: @compiled_rating, cool_var: 'hey gurl'} %><br>
  <br>

  <%= month_calendar(partial: 'simple_calendar/month_calendar', events: @occurrences[:dates]) do |date, occurrences| %>
    <%= date %>
    <% occurrences.each do |occurrence| %>
    <h1><%= @occurrence[:start_booked_times] %></h1>
    <div>
      Available from <%= @occurrences[:start_time] %> to <%= @occurrences[:end_time] %>
    </div>
    <% end %>
  <% end %>

<% end %>
